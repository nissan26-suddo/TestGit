# TestGit

This repository contains a simple Pygame-based Mario-style platformer.

## Requirements
- Python 3
- Pygame library (`pip install pygame`)

## Running the Game
After installing the requirements, run:
```bash
python3 mario.py
```

Use the arrow keys to move left and right. Press Space or the Up arrow to jump.
